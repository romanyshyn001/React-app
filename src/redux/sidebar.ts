let initiateState = {};

type initiateStateType = typeof initiateState;

const sideBarReducer = (
  state = initiateState,
  action: any
): initiateStateType => {
  return state;
};
export default sideBarReducer;
